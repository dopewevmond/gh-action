name: Deploy locally (Reusable)

on:
  workflow_call:
    inputs:
      env:
        required: true
        type: string
      runner_labels:
        required: true
        type: string
    secrets:
      DOCKER_CONTAINER_NAME: { required: true }
      NODE_ENV: { required: true }
      SERVER_PORT: { required: true }
      APP_NAME: { required: true }
      HOST_NAME: { required: true }

jobs:
  deploy:
    runs-on: ${{ fromJSON(inputs.runner_labels) }}
    environment: ${{ inputs.env }}
    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Tear Down Containers
        env:
          COMPOSE_PROJECT_NAME: ${{ inputs.env }}
          DOCKER_CONTAINER_NAME: ${{ secrets.DOCKER_CONTAINER_NAME }}
          NODE_ENV: ${{ secrets.NODE_ENV }}
          SERVER_PORT: ${{ secrets.SERVER_PORT }}
          APP_NAME: ${{ secrets.APP_NAME }}
          HOST_NAME: ${{ secrets.HOST_NAME }}
        run: |
            docker compose -p "gh-action-$COMPOSE_PROJECT_NAME" down

      - name: Deploy with Docker Compose
        env:
          COMPOSE_PROJECT_NAME: ${{ inputs.env }}
          DOCKER_CONTAINER_NAME: ${{ secrets.DOCKER_CONTAINER_NAME }}
          NODE_ENV: ${{ secrets.NODE_ENV }}
          SERVER_PORT: ${{ secrets.SERVER_PORT }}
          APP_NAME: ${{ secrets.APP_NAME }}
          HOST_NAME: ${{ secrets.HOST_NAME }}
        run: |
            docker compose -p "gh-action-$COMPOSE_PROJECT_NAME" up --build -d